# Alpine Userspace on Pedigree

This directory contains a builder setup for generating an ext2 filesystem with
a basic Alpine/musl-based Linux userspace.

This can then be used with Pedigree's Linux/POSIX subsystem to use something a
bit more familiar and focus more on kernel development than building userspace.
