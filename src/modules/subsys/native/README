libpedigree.so provides the Input API for userspace applications. It supports
pollable input events from /dev/input/pedigree, event inhibition and keymap loading.

The native IPC API has been retired. Applications use POSIX sockets and shared
memory for IPC. Native syscall service 3, including its former operation numbers,
remains reserved and must not be reassigned.
