[project]
name = "pedigree"
version = "0.1.0"
description = "The Pedigree Operating System (this pyproject file is for scripts - not a package itself)"
readme = "README.md"
requires-python = ">=3.13"
dependencies = []
