name: CI

on:
  push:
  pull_request:

permissions:
  contents: read

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  clang-format:
    name: clang-format
    runs-on: ubuntu-24.04
    steps:
      - name: Check out repository
        uses: actions/checkout@v4
        with:
          submodules: recursive
          persist-credentials: false

      - name: Set up LLVM 22
        uses: ZhongRuoyu/setup-llvm@eea6238baf53d41c1615923680d7a738da584318 # v0.2.0
        with:
          llvm-version: 22

      - name: Show LLVM tool versions
        run: |
          clang-format --version
          clang-tidy --version

      - name: Check src formatting
        run: scripts/clang-format.sh --dry-run --Werror

  buildutil-tests:
    name: buildutil testsuite
    runs-on: ubuntu-24.04
    steps:
      - name: Check out repository
        uses: actions/checkout@v4
        with:
          submodules: recursive
          persist-credentials: false

      - name: Install host build dependencies
        run: |
          sudo apt-get update
          sudo apt-get install --no-install-recommends -y \
            bison \
            clang \
            cmake \
            flex \
            libssl-dev \
            nasm \
            ninja-build

      - name: Configure native host utilities
        run: |
          cmake -S . -B build/host -G Ninja \
            -DCMAKE_BUILD_TYPE=Debug \
            -DPEDIGREE_WARNINGS=ON \
            -DPEDIGREE_BUILDUTILS_ASAN=OFF \
            -DPEDIGREE_BUILDUTILS_PROFILE=Speed

      - name: Build buildutil testsuite
        run: cmake --build build/host --target testsuite --parallel

      - name: Run CTest
        run: ctest --test-dir build/host --output-on-failure --no-tests=error

  clang-tidy:
    name: clang-tidy
    runs-on: ubuntu-24.04
    continue-on-error: true
    steps:
      - name: Check out repository
        uses: actions/checkout@v4
        with:
          submodules: recursive
          persist-credentials: false

      - name: Set tidy toolchain location
        run: echo "TIDY_TOOLCHAIN_ROOT=$RUNNER_TEMP/pedigree-clang-tidy-toolchain" >> "$GITHUB_ENV"

      - name: Set up LLVM 22
        uses: ZhongRuoyu/setup-llvm@eea6238baf53d41c1615923680d7a738da584318 # v0.2.0
        with:
          llvm-version: 22

      - name: Install host analysis dependencies
        run: |
          sudo apt-get update
          sudo apt-get install --no-install-recommends -y \
            bison \
            binutils \
            cmake \
            flex \
            g++ \
            gcc \
            libssl-dev \
            nasm \
            ninja-build

      - name: Show LLVM tool versions
        run: |
          clang --version
          clang-tidy --version
          run-clang-tidy --help | sed -n '1,12p'

      - name: Configure host utility exporters
        run: |
          cmake -S . -B build/host -G Ninja \
            -DCMAKE_BUILD_TYPE=Debug \
            -DPEDIGREE_WARNINGS=ON \
            -DPEDIGREE_BUILDUTILS_ASAN=OFF \
            -DPEDIGREE_BUILDUTILS_PROFILE=Speed

      - name: Build host utility exporters
        run: cmake --build build/host --target headerify ext2img keymap memorytracer --parallel

      - name: Prepare host compiler shim
        run: |
          set -euo pipefail

          mkdir -p "$TIDY_TOOLCHAIN_ROOT/bin"
          ln -s "$(command -v clang)" \
            "$TIDY_TOOLCHAIN_ROOT/bin/x86_64-pedigree-gcc"
          ln -s "$(command -v clang++)" \
            "$TIDY_TOOLCHAIN_ROOT/bin/x86_64-pedigree-g++"
          ln -s "$(command -v objcopy)" \
            "$TIDY_TOOLCHAIN_ROOT/bin/x86_64-pedigree-objcopy"
          ln -s "$(command -v strip)" \
            "$TIDY_TOOLCHAIN_ROOT/bin/x86_64-pedigree-strip"

      - name: Configure full hosted compilation database
        run: |
          cmake -S . -B build/tidy -G Ninja \
            -DCMAKE_TOOLCHAIN_FILE="$GITHUB_WORKSPACE/build-etc/cmake/pedigree_hosted.cmake" \
            -DPEDIGREE_TOOLCHAIN_ROOT="$TIDY_TOOLCHAIN_ROOT" \
            -DIMPORT_EXECUTABLES="$GITHUB_WORKSPACE/build/host/HostUtilities.cmake" \
            -DPEDIGREE_BUILD_USER_DIR=OFF \
            -DPEDIGREE_HOSTED_DYNAMIC_MODULES=ON \
            -DPEDIGREE_HOSTED_SMOKE_TESTS=OFF \
            -DPEDIGREE_WITH_INIT=OFF \
            -DPEDIGREE_WARNINGS=ON

      - name: Run clang-tidy across the hosted kernel and modules
        run: |
          set -euo pipefail

          cxx_version="$(g++ -dumpversion)"
          cxx_include_dir="/usr/include/c++/$cxx_version"
          cxx_target_include_dir="/usr/include/$(gcc -dumpmachine)/c++/$cxx_version"
          test -d "$cxx_include_dir"
          test -d "$cxx_target_include_dir"

          run-clang-tidy \
            -p build/tidy \
            -removed-arg=-march=k8 \
            -removed-arg="--sysroot=$TIDY_TOOLCHAIN_ROOT/x86_64-pedigree" \
            -extra-arg-before=--gcc-toolchain=/usr \
            -extra-arg-before=-isystem \
            -extra-arg-before="$cxx_include_dir" \
            -extra-arg-before=-isystem \
            -extra-arg-before="$cxx_target_include_dir" \
            -j 2

  builder-availability:
    name: resolve builder image
    runs-on: ubuntu-24.04
    permissions:
      contents: read
      packages: read
    outputs:
      image: ${{ steps.fingerprint.outputs.image }}
      available: ${{ steps.lookup.outputs.available }}
    steps:
      - name: Check out repository
        uses: actions/checkout@v4
        with:
          submodules: recursive
          persist-credentials: false

      - name: Compute builder image reference
        id: fingerprint
        shell: bash
        run: |
          set -euo pipefail

          fingerprint=$(bash scripts/ci/builder-fingerprint.sh)
          image="ghcr.io/${{ github.repository_owner }}/pedigree-builder:x86_64-${fingerprint}"
          echo "image=$image" >> "$GITHUB_OUTPUT"
          echo "Builder image: $image"

      - name: Log in to GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ github.token }}

      - name: Check for the fingerprinted image
        id: lookup
        env:
          BUILDER_IMAGE: ${{ steps.fingerprint.outputs.image }}
        run: |
          set -euo pipefail

          if docker manifest inspect "$BUILDER_IMAGE" >/dev/null 2>&1; then
            available=true
            echo "The fingerprinted builder image is already available."
          else
            available=false
            echo "The fingerprinted builder image is not available yet."
          fi
          echo "available=$available" >> "$GITHUB_OUTPUT"

  build-builder:
    name: build missing builder image
    needs: builder-availability
    if: >-
      needs.builder-availability.outputs.available != 'true' &&
      github.event_name == 'push' &&
      github.ref == 'refs/heads/develop'
    permissions:
      contents: read
      packages: write
    uses: ./.github/workflows/builder-image.yml

  cross-build:
    name: cross-compiled kernel and ISO
    needs:
      - clang-format
      - buildutil-tests
      - clang-tidy
      - builder-availability
      - build-builder
    if: >-
      always() &&
      needs.clang-format.result == 'success' &&
      needs.buildutil-tests.result == 'success' &&
      needs.clang-tidy.result == 'success' &&
      needs.builder-availability.result == 'success' &&
      (needs.build-builder.result == 'success' || needs.build-builder.result == 'skipped')
    permissions:
      contents: read
      packages: read
    runs-on: ubuntu-24.04
    timeout-minutes: 90
    steps:
      - name: Check out repository
        uses: actions/checkout@v4
        with:
          submodules: recursive
          persist-credentials: false

      - name: Log in to GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ github.token }}

      - name: Pull or build the matching builder image
        env:
          BUILDER_IMAGE: ${{ needs.builder-availability.outputs.image }}
        run: |
          set -euo pipefail

          if docker pull "$BUILDER_IMAGE"; then
            echo "Using the published builder image."
          else
            echo "No published builder image is available; building it for this run."
            docker build --pull \
              --file build-etc/docker/pedigree-builder.Dockerfile \
              --tag "$BUILDER_IMAGE" \
              .
          fi

          docker image inspect "$BUILDER_IMAGE" >/dev/null

      - name: Build the cross-compiled kernel and ISO
        env:
          BUILDER_IMAGE: ${{ needs.builder-availability.outputs.image }}
        run: |
          set -euo pipefail

          docker run --rm \
            --volume "$GITHUB_WORKSPACE:/workspace" \
            --workdir /workspace \
            "$BUILDER_IMAGE" \
            bash -euo pipefail -c '
              cmake -S . -B build/host -G Ninja \
                -DCMAKE_BUILD_TYPE=Debug \
                -DPEDIGREE_WARNINGS=ON \
                -DPEDIGREE_BUILDUTILS_ASAN=OFF \
                -DPEDIGREE_BUILDUTILS_PROFILE=Speed
              cmake --build build/host \
                --target headerify ext2img keymap memorytracer \
                --parallel

              cmake -S . -B build/cross -G Ninja \
                -DCMAKE_TOOLCHAIN_FILE=/workspace/build-etc/cmake/pedigree_amd64.cmake \
                -DPEDIGREE_TOOLCHAIN_ROOT=/opt/pedigree \
                -DIMPORT_EXECUTABLES=/workspace/build/host/HostUtilities.cmake \
                -DPEDIGREE_BUILD_USER_DIR=OFF \
                -DPEDIGREE_WARNINGS=ON
              cmake --build build/cross \
                --target livecd hddimage \
                --parallel
              ctest --test-dir build/cross --output-on-failure --no-tests=error
              test -s build/cross/pedigree.iso
              test -s build/cross/hdd.img
              test -s build/cross/src/system/kernel/kernel-mini64
              test -s build/cross/src/modules/initrd.tar
            '

      - name: Upload cross-build artifacts
        uses: actions/upload-artifact@v4
        with:
          name: pedigree-cross-build-${{ github.sha }}
          if-no-files-found: error
          path: |
            build/cross/pedigree.iso
            build/cross/hdd.img
            build/cross/src/system/kernel/kernel-mini64
            build/cross/src/modules/initrd.tar
